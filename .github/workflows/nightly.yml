name: Nightly validation

# Non-publishing scheduled and manual validation.
on:
  schedule:
    - cron: "17 4 * * *"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: nightly
  cancel-in-progress: false

jobs:
  resolve-latest-node:
    runs-on: ubuntu-latest
    permissions: {}
    outputs:
      version: ${{ steps.node.outputs.version }}
    steps:
      - name: Set up latest Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: node
      - name: Resolve exact Node version
        id: node
        run: echo "version=$(node -p 'process.versions.node')" >> "$GITHUB_OUTPUT"

  native-matrix:
    needs: resolve-latest-node
    strategy:
      fail-fast: false
      matrix:
        runner: [ubuntu-latest, macos-latest]
    uses: ./.github/workflows/validate.yml
    with:
      runner: ${{ matrix.runner }}
      node-version: ${{ needs.resolve-latest-node.outputs.version }}
      mode: full
      audit: ${{ matrix.runner == 'ubuntu-latest' }}
    permissions:
      contents: read
